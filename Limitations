* project create for puppet integration does not work with rd-project,
only through the gui

* if importing jobs through rd-jobs and starting the jobs, sometimes the job will fail.
the nodes must defined again .... ????

* 
